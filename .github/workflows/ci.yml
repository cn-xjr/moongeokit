name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  quality:
    name: Quality gates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup MoonBit
        uses: hustcer/setup-moonbit@v1

      # MoonBit 0.10.4 does not accept --deny-warn for fmt/info.  These are
      # their supported strict equivalents: checking formatting and refusing
      # generated public-interface drift.
      - name: Format check
        run: moon fmt --check

      - name: Check without warnings
        run: moon check --deny-warn --target all

      - name: Verify generated public API
        run: |
          moon info
          git diff --exit-code -- '*.mbti'

      - name: Test without warnings
        run: moon test --deny-warn --target all

  moonbit:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [native, js, wasm, wasm-gc]
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup MoonBit
        uses: hustcer/setup-moonbit@v1

      - name: Check without warnings
        run: moon check --deny-warn --target ${{ matrix.target }}

      - name: Format
        run: moon fmt --check

      - name: Run tests without warnings
        run: moon test --deny-warn --target ${{ matrix.target }}

      - name: Run CLI demo
        if: matrix.target == 'native'
        run: |
          moon run cmd/main
          moon run cmd/bench

      - name: Verify package
        if: matrix.target == 'native'
        run: moon package --list

      - name: Verify generated public API
        if: matrix.target == 'native'
        run: |
          moon info
          git diff --exit-code -- '*.mbti'
